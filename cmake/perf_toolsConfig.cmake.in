@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find dependencies
find_dependency(Threads)

# Include targets
include("${CMAKE_CURRENT_LIST_DIR}/perf_toolsTargets.cmake")

check_required_components(perf_tools)
